name: testing-actions

on:
    workflow_dispatch:


jobs:
    run-manually:
     runs-on: ubuntu-latest
     steps:
      - name: Print Environment
        run: echo "Running on environment- manual-dispatch"